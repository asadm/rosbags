/* Empty shim used during browser bundling to replace built-in Node modules
   that are not available in the browser (e.g. `fs`).  The writer gracefully
   handles a `null` value, so an empty object is sufficient. */

export default {};
